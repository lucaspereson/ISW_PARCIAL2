{
  "name": "mi-proyecto",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "jest",
    "build": "npm run lint && npm run transpile",
    "lint": "eslint src",
    "transpile": "babel src --out-dir dist"
  },
  "devDependencies": {
    "jest": "^27.0.6"
  }
}