{
  "name": "lsp-isw",
  "version": "1.0.0",
  "main": "dist/app.js",
  "scripts": {
    "start": "node dist/app.js",
    "test": "jest",
    "build": "npm run lint && npm run transpile",
    "lint": "eslint .",
    "transpile": "babel src -d dist"
  },
  "devDependencies": {
    "jest": "^27.0.6"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
